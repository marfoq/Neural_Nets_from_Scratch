from copy import deepcopy


# ToDo impalement test
def train(model, X_test, y_test):
    """
    Computes the accuracy of the model on test set
    :param model: network.NeuralNetwork object
    :param X_test: np.array of shape (n_samples, d)
    :param y_test: np.array of shape (n_samples,)
    :return: the accuracy of the model on the test set
    """
    return 0.5

